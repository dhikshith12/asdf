/*
    It is a dynamic sized array. Number of elements can be increased or decreased. (In Java same behaviour is shown by ArrayList).
*/
#include<vector>

std::vector<int> v; // empty vector of integers
std::vector<int> v1(10); //vector of integers with 10 elements (all 0)
std::vector<char> v2(10,'h'); // vector of char's with 10 elements (all 'h')

/* Important Functions */

